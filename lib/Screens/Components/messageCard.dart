import 'package:flutter/material.dart';

class FromMessageCard extends StatelessWidget {
  var data;
  FromMessageCard({super.key, required this.data});

  @override
  Widget build(BuildContext context) {
    return Container(
        // from message card code here
        );
  }
}

class ToMessageCard extends StatelessWidget {
  var data;
  ToMessageCard({super.key, required this.data});

  @override
  Widget build(BuildContext context) {
    return Container(
    
         //  to message card code here
        );
  }
}
